import { createSlice } from "@reduxjs/toolkit";

const ephesiansSlice = createSlice({
    name:'ephesians',
    initialState:[],
    reducers: {
        }
    }
);

export const ephesiansReducer = ephesiansSlice.reducer;